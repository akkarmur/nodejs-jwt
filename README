const crypto = require("crypto");

const cr=()=>{
    let cipher = crypto.createCipher('aes256',"secfkhfuhur")
    let encrypted = cipher.update(password,'utf8',"hex") //pass the password
    cipher.final('hex');
}

const dc = () =>{
    let decipher = crypto.createDecipher(algo,key);
    let decrypted =  decipher.update(password,'hex','utf8')
    dicipher.final('utf8')
    console.log(decrypted)
}